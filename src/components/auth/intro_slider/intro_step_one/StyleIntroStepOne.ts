import {StyleSheet} from 'react-native';
import {RW} from '../../../store/ExternalLibrary';

const styles = StyleSheet.create({
  intro_img: {
    aspectRatio: 1,
    resizeMode: 'contain',
    height: RW(6),
  },
});

export default styles;
